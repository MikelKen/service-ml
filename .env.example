# Application Configuration
APP_NAME=service_ml
APP_VERSION=1.0.0
DESCRIPTION=FastAPI ML Service with GraphQL

# Server Configuration
HOST=0.0.0.0
PORT=3001
DEBUG=true
ENVIRONMENT=development

# CORS Configuration
CORS_ORIGINS=*
CORS_METHODS=*
CORS_HEADERS=*

# GraphQL Configuration
GRAPHQL_PATH=/graphql
GRAPHIQL_ENABLED=true

# API Configuration
API_PREFIX=/api